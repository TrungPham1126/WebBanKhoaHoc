spring.application.name=course-service
server.port=8082

# --- FIX LỖI STARTUP ---
spring.config.import=optional:configserver:

# --- Tắt Cloud Config/Eureka để chạy Local đơn giản ---
spring.cloud.config.enabled=false
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# --- KẾT NỐI DATABASE (Localhost) ---
spring.datasource.url=jdbc:mysql://localhost:3306/course_service_db
spring.datasource.username=root
# Điền password MySQL của bạn vào đây
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA Config ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- File Upload Config ---
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# --- FIX LỖI "Could not resolve placeholder jwt.secret" ---
# (Phải dùng chung secret key với User Service để xác thực được token)
jwt.secret=daylachuoibimatcuaban9999999999999999999999999999999
jwt.expiration=86400000