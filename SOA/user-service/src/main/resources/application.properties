spring.application.name=user-service
server.port=8081

# --- QUAN TRỌNG: Tắt kết nối Cloud để chạy độc lập ---
spring.cloud.config.enabled=false
eureka.client.enabled=false

# --- Cấu hình Database (Localhost:3306) ---
spring.datasource.url=jdbc:mysql://localhost:3306/user_service_db
spring.datasource.username=root
# Điền password nếu có (mặc định XAMPP thường để trống)
spring.datasource.password=long2005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- Cấu hình JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- Cấu hình JWT ---
jwt.secret=daylachuoibimatcuaban9999999999999999999999999999999
jwt.expiration=86400000